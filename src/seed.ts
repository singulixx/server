import 'dotenv/config';
console.log('Seeding disabled by default. If you need to seed data, create a custom seed file or set an explicit environment variable and implement safe seeding.');
process.exit(0);

# --- PostgreSQL ---
# Replace user, password, host, port, and database as needed
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/singulix?schema=public"

# Optional shadow DB for Prisma migrations (recommended for CI)
# SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/singulix_shadow?schema=public"
# --- PostgreSQL (Vercel Postgres / Neon) ---
DATABASE_URL="postgresql://USER:PASS@HOST/DB?sslmode=require&pgbouncer=true&connect_timeout=15"
# DIRECT_URL="postgresql://USER:PASS@HOST/DB?sslmode=require"
# --- Auth & CORS ---
JWT_SECRET="change-me"
CORS_ORIGIN="http://localhost:3000"
# --- Storage ---
BLOB_READ_WRITE_TOKEN=""
# --- Cron ---
CRON_SECRET=""
